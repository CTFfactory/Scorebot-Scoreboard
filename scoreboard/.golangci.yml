run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gocyclo
    - gosec
    - govet
    - ineffassign
    - revive
    - staticcheck
    - unused

linters-settings:
  gocyclo:
    min-complexity: 6

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
